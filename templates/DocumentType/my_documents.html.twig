{% block body %}
    <h1>{{ 'header.documents.your_document_types'|trans([], 'labels') }}</h1>

    <table>
        <thead>
        <tr>
            <th>{{ 'header.my_document_types.table.columns.id'|trans }}</th>
            <th>{{ 'header.my_document_types.table.columns.type'|trans }}</th>
            <th>{{ 'header.my_document_types.table.columns.actions'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for document_type in document_types %}
            <tr>
                <td>{{ document_type.documentId() }}</td>
                {# set property = document.getPublicProperty('type') #}
                <td>
{#                    <span>{{ property.getLabel() }}</span>#}
{#                    <span>{{ property.getType() }}</span>#}
                </td>
                <td>
                    <span>
{#                        <a href="{{ url('document_design', {'id': document.getDocumentId()}) }}">{{ document.getName() }}</a>#}
                    </span>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
